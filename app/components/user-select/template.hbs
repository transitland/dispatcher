{{#power-select
  options=usersToSelectFrom
  selected=userToSet
  matcher=userMatcher
  onchange=(action (mut userToSet))
  allowClear=true
  as |user|}}

  {{user.name}} &lt;{{user.email}}&gt;

{{/power-select}}
