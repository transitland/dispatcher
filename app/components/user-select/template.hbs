{{#power-select
  options=usersToSelectFrom
  selected=userToSet
  matcher=userMatcher
  onchange=(action (mut userToSet))
  onfocus=(action "handleFocus")
  allowClear=true
  as |user|}}

  {{user.name}} &lt;{{user.email}}&gt;

{{/power-select}}
