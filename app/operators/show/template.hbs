<h2>{{link-to 'Operators' 'operators'}} / {{model.onestop_id}}</h2>

<dl class="dl-horizontal">
  <dt>Name</dt>
  <dd>{{model.name}}</dd>
  <dt>Short name</dt>
  <dd>{{model.short_name}}</dd>
  <dt>Feeds</dt>
  <dd>
    {{#each feeds as |feed|}}
      {{feed.onestop_id}}
    {{/each}}
  </dd>
</dl>