<div class="row">
  <div class="col-md-9">
    <div class="form-group">
      <label>Issue Type</label>
      {{#power-select options=model.issueTypes searchField="display" selected=issue_type onchange=(action "typeChanged") allowClear=true as |issueType|}}
        {{issueType}}
      {{/power-select}}
    </div>
    {{#issue-table model=model route="issues.station-hierarchy.show"}}{{/issue-table}}
    {{pagination-controls route="issues.station-hierarchy.index" hasPreviousPage=hasPreviousPage hasNextPage=hasNextPage previousOffset=previousOffset nextOffset=nextOffset perPage=per_page}}
  </div>
</div>
