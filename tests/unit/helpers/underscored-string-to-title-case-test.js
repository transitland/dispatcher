import { underscoredStringToTitleCase } from '../../../helpers/underscored-string-to-title-case';
import { module, test } from 'qunit';

module('Unit | Helper | underscored string to title case');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = underscoredStringToTitleCase(42);
  assert.ok(result);
});
