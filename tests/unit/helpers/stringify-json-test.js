import { stringifyJson } from '../../../helpers/stringify-json';
import { module, test } from 'qunit';

module('Unit | Helper | stringify json');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = stringifyJson(42);
  assert.ok(result);
});
