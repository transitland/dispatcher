
import { toFixed } from 'dispatcher/helpers/to-fixed';
import { module, test } from 'qunit';

module('Unit | Helper | to fixed');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toFixed([42]);
  assert.ok(result);
});

